// Variável com um valor fixo. (Não pode ser alterado)
const number = 42
console.log(number)

// Isso aqui vai dar erro, pois o valor de uma const não pode ser alterado.
number = 55
console.log(number)